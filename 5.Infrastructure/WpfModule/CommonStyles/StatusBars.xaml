<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/CommonStyles;component/DefaultColorsAndBrushes.xaml" />
        <ResourceDictionary Source="/CommonStyles;component/DefaultControl.xaml" />
    </ResourceDictionary.MergedDictionaries>
    
    <!-- БАЗОВЫЙ СТАТУС-БАР -->
    <!-- Стиль -->
    <Style x:Key="StatusBarDefault" TargetType="{x:Type StatusBar}">
        <Setter Property="Background" Value="{StaticResource StatusBarBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource StatusBarForegroundBrush}"/>
    </Style>
    
</ResourceDictionary>